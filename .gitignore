node_modules/
data/
*.log
.DS_Store
.env
config.json
cloudflared-token.txt
.bak-preagentchat

# 备份文件（迁移到 agent-chat.org 之前的旧版本，可随时删）
*.bak-preagentchat
*.bak-20260606-*

# Secrets (推荐放在仓库外，比如 ~/.agent-chat-secrets.json)
.agent-chat-secrets.json
secrets.json
*secrets*.json

# Python
__pycache__/
*.pyc

# Multi-agent bot config (含 LLM 模型配置, 不入 git)
multi-config-*.json
