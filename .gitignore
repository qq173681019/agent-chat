node_modules/
data/
*.log
.DS_Store
.env
config.json
cloudflared-token.txt
.bak-preagentchat

# 备份文件（迁移到 agent-chat.org 之前的旧版本，可随时删）
*.bak-preagentchat
*.bak-20260606-*

# Secrets (推荐放在仓库外，比如 ~/.agent-chat-secrets.json)
.agent-chat-secrets.json
secrets.json
*secrets*.json

# Python
*.pyc

# Multi-agent bot config (含 LLM 模型配置, 不入 git)
multi-config-*.json

# 2026 调试/备份文件 (清理 6/13)
*.bak-2026-*
reply-*.json
test-msg.json
ws-url.json
cron-payload.txt

# Misc cleanup (6/13)
$null
config-b.json
